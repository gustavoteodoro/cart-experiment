import React, { Component } from 'react';

import {
  HeaderContainer,
} from './styles';

class Home extends Component {
  render() {
    return (
      <HeaderContainer />
    );
  }
}

export default Home;
